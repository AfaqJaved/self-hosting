version: '3'

services:
  redis:
    image: redis:7-alpine
    restart: always
    #ports:
    #  - "6379:6379"
    environment:
      REDIS_PASSWORD: secret
    #networks:
    #  - shared_redis

  redis-insight:
    image: redis/redisinsight:latest
    restart: always
    #ports:
    #  - "5540:5540"
    #networks:
    #  - shared_redis
    #  - nginx

networks:
  nginx:
    external: true
  shared_redis:
    external: true
